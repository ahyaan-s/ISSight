# ISSight

ISSight is a real-time International Space Station tracking and pass prediction web application built with Python and orbital data.

## Current Features

- Fetches current ISS latitude and longitude
- Updates position once per second

## Planned Features

- Interactive world map with animated marker of the current ISS location
- Predictions of passes over the users location
- Flask web interface
